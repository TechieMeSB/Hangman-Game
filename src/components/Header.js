import React, { Fragment } from "react";

const Header = () => {
  return (
    <Fragment>
      <h1>Hangman</h1>
      <p>Find the word </p>
      <p>Enter a letter and guess the word</p>
    </Fragment>
  );
};

export default Header;